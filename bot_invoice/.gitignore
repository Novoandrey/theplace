# bot_invoice — секреты и временные/чувствительные артефакты

# секреты (появятся в v1 — бот)
.env
.env.*
!.env.example

# выгрузки из Quick Resto (свежие справочники/документы — зеркала, не версионируем сырьё)
выгрузки/
exports/
*_export_raw.*

# изображения/PDF накладных (могут содержать реквизиты) — не коммитим
накладные/
invoices/
*.heic
# (примеры в samples/ — оставляем намеренно)

# python
__pycache__/
*.pyc
.venv/
venv/
